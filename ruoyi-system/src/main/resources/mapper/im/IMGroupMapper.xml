<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.im.mapper.IMGroupMapper">
	<select id="check" parameterType="com.ruoyi.common.core.domain.PageData" resultType="com.ruoyi.common.core.domain.PageData">
		select id from im_my_group where user_id = #{userId} and id = #{groupId}
	</select>
</mapper> 